# token-demo-scratch

A tiny JWT-style auth helper: `signJwt` signs a payload, `verifyJwt` checks it round-trips.

## Run the tests

```bash
npm test    # Node's built-in runner (node --test) — no dependencies, Node 18+
```
